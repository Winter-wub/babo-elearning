/** localStorage key prefix for per-page tour completion flags */
export const TOUR_SEEN_PREFIX = "admin-tour-seen:";

/** localStorage key for global "skip all tours" flag */
export const TOUR_DISMISSED_KEY = "admin-tour-dismissed";

/** Delay (ms) before auto-starting a tour to let the DOM hydrate */
export const TOUR_AUTO_START_DELAY = 600;
